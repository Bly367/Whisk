# Whisk local / EAS secrets — copy to `.env` (never commit real values).

# Optional: point the app at the shared sync server for real multi-device sessions.
# Start with: npm run sync-server
# EXPO_PUBLIC_WHISK_SYNC_URL=http://localhost:8787

# Optional future: Supabase Auth + RLS (SECURITY.md §6) when a project is provisioned.
# EXPO_PUBLIC_SUPABASE_URL=
# EXPO_PUBLIC_SUPABASE_ANON_KEY=
